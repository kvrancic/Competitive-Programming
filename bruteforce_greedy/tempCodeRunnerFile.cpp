
        if(c == x) return true; 
    }
    return false;
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    // Rješenje vašeg zadatka
    int n; 
    cin >> n;
    map<double, string> sokovi;  
    for(int i = 0; i < n; i++){